# ----- Serveur -----
server.port=8080

# ----- Datasource MySQL -----
spring.datasource.url=jdbc:mysql://localhost:3306/annuaire_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=JESUSglory_7

# ----- JPA / Hibernate -----
spring.jpa.hibernate.ddl-auto=update
    ##pour afficher les tables lorsqu'elles sont nouvellement créé et non update
spring.jpa.show-sql=true
#spring.jpa.open-in-view=true
spring.jpa.properties.hibernate.format_sql=true
    ##pour exécuter les scripts sql
#spring.sql.init.mode= always

# (optionnel) Dialecte explicite MySQL 8
#----la ligne ci-dessous est en commentaire parce qu'elle est autodétecté
#spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
